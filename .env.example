# Telegram Bot
BOT_TOKEN=your_telegram_bot_token_here
WEBHOOK_URL=https://your-app.railway.app

# Database (Railway provides this automatically)
DATABASE_URL=postgresql+asyncpg://user:password@localhost:5432/yurist_bot

# AI — Sber GigaChat (https://developers.sber.ru/studio)
# GIGACHAT_AUTH_KEY — «Authorization Key» из личного кабинета (длинная base64-строка).
GIGACHAT_AUTH_KEY=your_authorization_key_here
# Scope: GIGACHAT_API_PERS (физлицо) | GIGACHAT_API_B2B | GIGACHAT_API_CORP
GIGACHAT_SCOPE=GIGACHAT_API_PERS
GIGACHAT_MODEL=GigaChat-2-Max
# Сертификаты Сбера от НУЦ Минцифры обычно не в системном хранилище → оставьте false
GIGACHAT_VERIFY_SSL=false

# Владельцы — создаются при старте если их ещё нет в staff-таблице.
# OWNER_BOOTSTRAP_TELEGRAM_ID — список ID через запятую (один или несколько).
# Первый ID получит логин OWNER_BOOTSTRAP_LOGIN, остальные — <login>_<id>.
# Все стартуют с одним паролем; каждый меняет свой через панель после входа.
# Можно добавить новый ID и рестартнуть — он дозаведётся как ещё один владелец.
# Свой telegram_id можно узнать через @userinfobot в Telegram.
OWNER_BOOTSTRAP_LOGIN=owner
OWNER_BOOTSTRAP_PASSWORD=change_me_to_a_long_random_secret
OWNER_BOOTSTRAP_TELEGRAM_ID=123456789,987654321

# App
WEBAPP_URL=https://your-app.railway.app
PORT=8000
HOST=0.0.0.0

# Upload settings
UPLOAD_DIR=uploads
MAX_FILE_SIZE=52428800
